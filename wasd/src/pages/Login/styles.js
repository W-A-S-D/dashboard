import Image from '../../img/background.jpg';
import Logo from '../../img/logo-original.svg';

export const styles = {
    container: {
        backgroundImage: `url(${Image})`,
        backgroundSize: 'cover',
        height: '100vh',
        width: '100vw',
    }
};

export const item = {
    img: `${Logo}`,
    title: 'Logo',
}
